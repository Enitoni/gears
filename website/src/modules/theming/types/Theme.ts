export interface Theme {
  colors: {
    primary: string
    accent: string
  }
  fontColors: {
    normal: string
    muted: string
  }
  transparencies: {
    positive: string
    negative: string
  }
  durations: {
    normal: string
  }
}
